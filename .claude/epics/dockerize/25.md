---
name: CIへのdocker buildビルド検証ステップ追加
status: closed
created: 2026-08-18T15:51:12Z
updated: 2026-08-18T15:58:00Z
github: https://github.com/Sut103/EvePing-for-Discord/issues/25
depends_on: [22]
parallel: true
conflicts_with: []
---

# Task: CIへのdocker buildビルド検証ステップ追加

## Description

既存の `.github/workflows/ci.yml` に `docker build .` を実行するステップ(またはジョブ)を追加し、Dockerfileのビルド失敗をPull Request時点で検知できるようにする。イメージのタグ付け・レジストリへのpush・公開は行わない(スコープ外)。

## Acceptance Criteria

- [ ] `.github/workflows/ci.yml` に `docker build .` を実行するstep(または新規job)が追加されている
- [ ] 既存の `test` ジョブと同じトリガー(push: main, pull_request: main)で実行される
- [ ] イメージのpush・レジストリへの公開を行うstepが含まれない

## Test Plan

- [ ] `TestCIWorkflowRunsDockerBuild` — `.github/workflows/ci.yml` を読み込み、`docker build` を実行するstepが存在することを確認(red: 未追加で失敗 → green: 追加後成功)
- [ ] `TestCIWorkflowDoesNotPushImage` — `.github/workflows/ci.yml` に `docker push` や `docker/login-action` 等、レジストリへのpushを示す記述が含まれないことを確認

## Technical Details

- 変更ファイル: `.github/workflows/ci.yml`
- 新規ファイル: `dockerbuild/ci_workflow_test.go`
- 既存の `test` ジョブに `docker build .` ステップを追記するか、独立した `docker-build` ジョブを追加する(GitHub Actions Ubuntuランナーは標準でDockerデーモンが使用可能なため、追加のセットアップは不要)。

## Dependencies

- Task 1(Dockerfileが存在し実際にビルド可能であること)

## Effort Estimate

- Size: XS
- Hours: 1

## Definition of Done

- [ ] Failing tests written first (red)、期待した理由で失敗することを確認した
- [ ] 実装(CIワークフロー変更)によりTest Planの全テストがgreenになる
- [ ] `go test ./...` が成功する
- [ ] Code reviewed
