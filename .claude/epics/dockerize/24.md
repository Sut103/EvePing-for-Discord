---
name: READMEへのDockerビルド・起動手順の追記
status: open
created: 2026-08-18T15:51:12Z
updated: 2026-08-18T15:58:00Z
github: https://github.com/Sut103/EvePing-for-Discord/issues/24
depends_on: [22, 23]
parallel: true
conflicts_with: []
---

# Task: READMEへのDockerビルド・起動手順の追記

## Description

`README.md` に、Dockerでのビルド・起動手順(`docker build`/`docker run`)および `docker-compose.yml` を使った起動手順を追記する。既存の「セットアップ」節のスタイルに合わせ、実際のDockerfile/docker-compose.ymlの内容(サービス名・環境変数名)と一致させる。

## Acceptance Criteria

- [ ] README に Dockerでのビルドコマンド(`docker build -t eveping .`)と起動コマンド(`docker run -e EVEPING_DISCORD_TOKEN=... eveping`)の記載がある
- [ ] README に `docker-compose.yml` を使った起動手順の記載がある
- [ ] 記載されたコマンド例が実際の `Dockerfile`/`docker-compose.yml` の内容(サービス名・環境変数名)と一致している

## Test Plan

- [ ] `TestReadmeDocumentsDockerBuildAndRun` — `README.md` に `docker build` と `docker run` を含む記述、および `EVEPING_DISCORD_TOKEN` への言及があることを確認(red: 未記載で失敗 → green: 追記後成功)
- [ ] `TestReadmeDocumentsDockerCompose` — `README.md` に `docker compose`(または `docker-compose`)への言及があることを確認

## Technical Details

- 変更ファイル: `README.md`
- 新規ファイル: `dockerbuild/readme_test.go`
- 既存の「セットアップ」節の後に「Dockerでの起動」節を新設して追記する。

## Dependencies

- Task 1(Dockerfile)、Task 2(docker-compose.yml) — コマンド例が実物と一致している必要があるため、両方の内容が確定していること

## Effort Estimate

- Size: XS
- Hours: 1

## Definition of Done

- [ ] Failing tests written first (red)、期待した理由で失敗することを確認した
- [ ] 実装(README追記)によりTest Planの全テストがgreenになる
- [ ] `go test ./...` が成功する
- [ ] Code reviewed
