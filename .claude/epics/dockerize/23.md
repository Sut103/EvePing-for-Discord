---
name: 開発用docker-compose.ymlの追加
status: closed
created: 2026-08-18T15:51:12Z
updated: 2026-08-18T15:58:00Z
github: https://github.com/Sut103/EvePing-for-Discord/issues/23
depends_on: [22]
parallel: true
conflicts_with: []
---

# Task: 開発用docker-compose.ymlの追加

## Description

ローカル開発でビルド〜起動を一発で行える `docker-compose.yml` をリポジトリルートに追加する。Task 1で追加する `Dockerfile` をビルドコンテキストとして参照し、`EVEPING_DISCORD_TOKEN` は `.env` 等の環境変数経由でのみ注入する(値をファイルにハードコードしない)。

## Acceptance Criteria

- [ ] リポジトリルートに `docker-compose.yml` が追加され、Task 1の `Dockerfile` をビルドコンテキストとして参照する
- [ ] `EVEPING_DISCORD_TOKEN` は `environment`/`env_file` を通じて注入され、値がハードコードされていない
- [ ] `docker compose config` で構文エラーなく解釈できる(静的検証)

## Test Plan

- [ ] `TestComposeFileReferencesTokenEnvVar` — `docker-compose.yml` に `EVEPING_DISCORD_TOKEN` への参照(`environment` のキーとしての記載、または `env_file` 指定)が含まれ、値がハードコードされていないことを確認(red: `docker-compose.yml` 不在で失敗 → green: 追加後成功)
- [ ] `TestComposeFileBuildsFromRepoDockerfile` — `docker-compose.yml` の `build` セクションがリポジトリルート(`.`)を指していることを確認

## Technical Details

- 新規ファイル: `docker-compose.yml`, `dockerbuild/compose_test.go`
- 例:
  ```yaml
  services:
    eveping:
      build: .
      environment:
        - EVEPING_DISCORD_TOKEN=${EVEPING_DISCORD_TOKEN}
      restart: unless-stopped
  ```
- ローカルの `.env`(コミットしない、`.gitignore` で既に除外済み)に `EVEPING_DISCORD_TOKEN=...` を書く運用を想定する。

## Dependencies

- Task 1(Dockerfile) — ビルドコンテキストとして参照するため、先に確定している必要がある

## Effort Estimate

- Size: XS
- Hours: 1

## Definition of Done

- [ ] Failing tests written first (red)、期待した理由で失敗することを確認した
- [ ] 実装(`docker-compose.yml`)によりTest Planの全テストがgreenになる
- [ ] `go build ./...` / `go vet ./...` / `go test ./...` が全て成功する
- [ ] (Docker環境がある場合)手動で `docker compose config` および `docker compose up` を実行し確認した
- [ ] Code reviewed
