---
name: リマインドメッセージ生成
status: open
created: 2026-08-17T15:56:15Z
updated: 2026-08-17T15:56:15Z
github: (will be set on sync)
depends_on: [1]
parallel: true
conflicts_with: []
---

# Task: リマインドメッセージ生成

## Description

`internal/reminder` パッケージに、イベント情報（名前・開始日時・イベントID・ギルドID）からDM本文を組み立てる `FormatReminder(event discordclient.Event) string` を実装する。文面にはイベント名・開始日時（人間が読みやすい形式）・イベントへのリンクURLを含める。

## Acceptance Criteria
- [ ] 生成されたメッセージにイベント名が含まれる
- [ ] 生成されたメッセージに開始日時が人間が読める形式で含まれる
- [ ] 生成されたメッセージにイベントへのDiscordリンクURL（`https://discord.com/events/<guildID>/<eventID>` 形式）が含まれる

## Test Plan
- [ ] 通常のイベント（名前・開始日時・ID一式が揃っている）を渡した際に、期待する文言・URLを含む文字列が生成されることを確認するテスト — 上記3つの受け入れ基準すべてを担保

## Technical Details

- URL形式: `https://discord.com/events/{guildID}/{eventID}`
- 日時表示はUTCで固定し、フォーマットは実装時に決める（例: `2026-08-18 20:00 UTC`）。

## Dependencies

- Task 1（`internal/discordclient` の `Event` 型定義が前提）

## Effort Estimate
- Size: XS
- Hours: 1

## Definition of Done
- [ ] Failing tests written first (red), one per Test Plan item, and confirmed to fail for the right reason
- [ ] Implementation makes all Test Plan tests pass (green)
- [ ] Full relevant test suite passes — no other tests broken
- [ ] Code reviewed
